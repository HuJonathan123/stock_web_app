name: Daily Stock Data Update

# 設定觸發時機
on:
  schedule:
    # 每天 UTC 時間 22:00 (對應台灣/香港時間 早上 06:00)
    - cron: '0 22 * * *'
  # 允許手動按按鈕觸發 (方便測試)
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update_data:
    runs-on: ubuntu-latest

    steps:
    # 1. 下載你的程式碼
    - name: Checkout repository
      uses: actions/checkout@v3

    # 2. 設定 Python 環境
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    # 3. 安裝套件
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        # 這裡直接安裝需要的套件，省去 requirements.txt 的麻煩
        pip install pandas yfinance streamlit

    # 4. 執行回測腳本 (這會生成新數據和更新時間)
    - name: Run Backtest Script
      run: python run_backtest.py

    # 5. 把新數據推回 GitHub
    - name: Commit and push if changes
      run: |
        git config --global user.name 'GitHub Actions Bot'
        git config --global user.email 'actions@github.com'
        git add data/*.csv data/*.json
        # 檢查有沒有變更，有變更才上傳
        git diff --quiet && git diff --staged --quiet || (git commit -m "🤖 Daily auto-update data" && git push)