name: Daily AI Stock Update

on:
  schedule:
    # 每天 UTC 時間 22:30 執行 (台灣時間早上 6:30)
    - cron: '30 22 * * 1-5'
  workflow_dispatch: # 允許手動點擊執行

jobs:
  update-data:
    runs-on: ubuntu-latest
    # 🔥 環境變數：隱藏 GPU 警告，並將你的 GitHub Secrets 傳入 Python 讓寄信功能可以運作！
    env:
      TF_CPP_MIN_LOG_LEVEL: '3'
      CUDA_VISIBLE_DEVICES: '-1'
      EMAIL_USER: ${{ secrets.hujohnnetha@gmail.com }}
      EMAIL_PASSWORD: ${{ secrets.srzxywqroexuijvm }}

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        # 強制安裝所有必備套件，包含 ta
        pip install ta --upgrade yfinance pandas numpy scikit-learn tensorflow-cpu streamlit
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

    - name: Run Market Scanner & Backtests
      run: |
        # ==========================================
        # 🦅 更新 Tab 1 & Tab 2 (經典與超級禿鷹)
        # ==========================================
        python run_backtest.py

        # ==========================================
        # 🧠 更新 Tab 3 & 4 (每日最新 AI 推薦名單)
        # ==========================================
        python ai_market_scanner.py
        
        # ==========================================
        # 📜 更新 Tab 3 & 4 (歷史回測資產曲線)
        # ==========================================
        python ai_backtest_2.py
        python ai_backtest_ma30_2.py

    - name: Commit and Push changes
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action Bot"
        git add data/
        git commit -m "🤖 Auto-update daily stock data" || exit 0
        git push