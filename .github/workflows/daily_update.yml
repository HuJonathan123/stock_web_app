name: Daily Stock Data Update

on:
  schedule:
    - cron: '0 22 * * *'  # 每天 UTC 22:00 (台灣時間 06:00)
  workflow_dispatch:      # 允許手動執行

permissions:
  contents: write

jobs:
  update_data:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pandas yfinance streamlit

    # 4. 執行回測腳本 (這會生成新數據和更新時間)
    - name: Run Backtest Script
      env:  # 🔥 新增這兩行，把密碼傳進去
        EMAIL_USER: ${{ secrets.HUJOHN }}
        EMAIL_PASSWORD: ${{ secrets.Hu200301 }}
      run: python run_backtest.py

    - name: Commit and push if changes
      run: |
        git config --global user.name 'GitHub Actions Bot'
        git config --global user.email 'actions@github.com'
        git add data/*.csv data/*.json
        git diff --quiet && git diff --staged --quiet || (git commit -m "🤖 Daily auto-update data" && git push)